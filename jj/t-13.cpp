#include <iostream>
#include <conio.h>
#include <string>
using namespace std;
int main()
{
    string s;

    system("cls");
    cout << "Please enter str: ";
    getline(cin, s);
    for (int i = 0; s[i]; i++)
    {
        if (s[i] >= 0 && s[i] = < 9)
            dgt++;
        else if (s[i] >= 'a' && s[i] = < 'z')
            k++;
        else if (s[i] >= 'A' && s[i] = < 'Z')
            b++;
            if (s[i]=='i'||s[i] ='I')
    }

    system("pause");
    return 0;
}